{
  "version": 2,
  "builds": [
    {
      "src": "firefly-dashboard/package.json",
      "use": "@vercel/next",
      "config": { "distDir": "firefly-dashboard/.next" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/firefly-dashboard/$1" }
  ]
}
